import { checkIconAvailability } from './checkIconAvailability';
import { checkIconConflicts } from './checkIconConflicts';
import { checkIconUsage } from './checkIconUsage';

if (false) {
  checkIconAvailability();
  checkIconUsage();
  checkIconConflicts();
}